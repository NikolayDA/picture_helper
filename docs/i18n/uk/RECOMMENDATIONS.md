[Deutsch](../../../RECOMMENDATIONS.md) · [English](../en/RECOMMENDATIONS.md) · [Español](../es/RECOMMENDATIONS.md) · [Français](../fr/RECOMMENDATIONS.md) · **Українська** · [简体中文](../zh/RECOMMENDATIONS.md)

# Аналіз коду та оцінені рекомендації: BgRemover

## Шкала оцінювання

| Символ | Пріоритет | Значення |
|--------|-----------|----------|
| 🔴 | Критичний | Потрібно виправити – призводить до помилок, збоїв або неузгодженостей |
| 🟠 | Високий | Слід виправити найближчим часом – суттєво знижує надійність або зручність супроводу |
| 🟡 | Середній | Рекомендовано – покращує якість коду, читабельність або тестованість |
| 🟢 | Низький | Необов'язково – шліфування, додаткові покращення |

---

## Підсумок за пріоритетами

| # | Рекомендація | Пріоритет | Трудовитрати |
|---|-----------|-----------|---------|
| 1 | ~~Конфлікт версій Python у підказках типів~~ | ✅ Виправлено | – |
| 2 | ~~Надто широке перехоплення винятків при імпорті rembg~~ | ✅ Виправлено | – |
| 3 | ~~Стани гонки у потоках-воркерах~~ | ✅ Виправлено | – |
| 4 | ~~Перевірка розміру зображення під час завантаження~~ | ✅ Виправлено | – |
| 5 | ~~Споживання пам'яті стеком скасувань~~ | ✅ Виправлено | – |
| 6 | ~~Розбиття класів-«богів»~~ | ✅ Виправлено | – |
| 7 | ~~Рефакторинг надто довгих методів~~ | ✅ Виправлено | – |
| 8 | ~~Заміна «магічних чисел»~~ | ✅ Виправлено | – |
| 9 | ~~Тести для сценаріїв із потоками~~ | ✅ Виправлено | – |
| 10 | ~~Додавання підказок типів для значень, що повертаються~~ | ✅ Виправлено | – |
| 11 | ~~Додавання docstring-ів~~ | ✅ Виправлено | – |
| 12 | ~~Платформонезалежний шлях до файлу журналу~~ | ✅ Виправлено | – |
| 13 | ~~Усунення дублювання шаблонного коду потоків~~ | ✅ Виправлено | – |

---

## Рекомендації детально

### ✅ 1. Конфлікт версій Python у підказках типів *(виправлено)*

**Файл**: `pyproject.toml`

`requires-python` піднято до `>=3.10`, `ruff target-version` оновлено до `py310`. Використовуваний у коді синтаксис `X | Y` (PEP 604) тепер покритий задекларованими мінімальними вимогами.

---

### ✅ 2. Надто широке перехоплення винятків при імпорті rembg *(виправлено)*

**Файл**: `BgRemover.py` (рядок 41)

`except BaseException:` замінено на `except (ImportError, RuntimeError, OSError, SystemExit):`. `KeyboardInterrupt` та інші критичні сигнали більше не перехоплюються. `SystemExit` залишено явно, оскільки відомі версії rembg/onnxruntime можуть викликати його під час імпорту.

---

### ✅ 3. Стани гонки у потоках-воркерах *(виправлено)*

**Файл**: `BgRemover.py`

- Новий помічник `_launch_worker()` у `MainWindow` інкапсулює однаковий шаблонний код потоку (раніше був продубльований тричі). Усі три потоки (Image Load, AI, Warmup) тепер використовують його.
- Перевірка застарілості в `_on_ai_done()` тепер використовує `_canvas._version` (монотонний цілочисловий лічильник, який збільшується при кожній зміні зображення в `apply_loaded_image()`) замість крихкого порівняння ідентичності об'єктів через `is`. `_ai_input_version` у `MainWindow` зберігає значення на момент запуску ШІ.

---

### ✅ 4. Відсутня перевірка розміру зображення під час завантаження *(виправлено)*

**Файл**: `BgRemover.py`

Введено константу `_MAX_MEGAPIXELS = 100`. Перевірка після лінивого `Image.open()` у двох місцях:
- `ImageLoadWorker.run()`: випромінює сигнал `error` з повідомленням про помилку (шлях через діалог файлів)
- `ImageCanvas.load_image()`: випромінює `statusMsg` і перериває (шлях drag-&-drop)

---

### ✅ 5. Високе споживання пам'яті стеком скасувань *(виправлено)*

**Файл**: `BgRemover.py`

Введено константу `_UNDO_MEMORY_LIMIT = 256 MB`. Стек скасувань більше не має жорсткого `maxlen` – натомість після кожного push обчислюється загальний розмір (оцінюється як `width × height × 4` байтів на RGBA-зображення), і найстаріші записи видаляються, доки ліміт перевищено.

---

### ✅ 6. Розбиття класів-«богів» *(виправлено)*

**Файл**: `BgRemover.py`

6 вкладених допоміжних функцій із `_build_right_panel()` (`sec`, `lbl`, `hdivider`, `scroll_tab`, `btn`, `slider_row`) винесено як методи класу `MainWindow` з декоратором `@staticmethod`: `_make_section`, `_make_label`, `_make_hdivider`, `_make_scroll_tab`, `_make_panel_btn`, `_make_slider`. `_TAB_STYLE` винесено як атрибут класу.

---

### ✅ 7. Рефакторинг надто довгих методів *(виправлено)*

**Файл**: `BgRemover.py`

8 гілок малювання іконок із `make_tool_icon()` (175 рядків, каскад if-elif) винесено як окремі функції модуля: `_draw_wand_icon`, `_draw_brush_icon`, `_draw_eraser_icon`, `_draw_ai_icon`, `_draw_open_icon`, `_draw_save_icon`, `_draw_undo_icon`, `_draw_restore_icon`. `make_tool_icon()` тепер є лаконічним диспетчером через `dict`.

---

### ✅ 8. Заміна «магічних чисел» іменованими константами *(виправлено)*

**Файл**: `BgRemover.py`

Новий блок констант у заголовку модуля:
- Компонування UI: `_TOOLBAR_WIDTH`, `_TOOLBAR_BTN_SIZE`, `_TOOLBAR_ICON_SIZE`, `_RIGHT_PANEL_WIDTH`, `_CROP_BAR_HEIGHT`, `_HISTORY_LIST_H`, `_COLOR_BTN_SIZE`, `_TAB_ICON_PX`, `_WINDOW_MIN_W/H`
- Типові значення canvas: `_DEFAULT_TOLERANCE`, `_DEFAULT_BRUSH_RADIUS`, `_ZOOM_FACTOR`
- Колір накладання: `_OVERLAY_COLOR`

Усі місця використання в коді переведено на ці константи.

---

### ✅ 9. Тести для шляхів помилок воркерів *(виправлено)*

**Файл**: `tests/test_workers.py` (новий, 9 тестів)

Нові тести:
- `ImageLoadWorker`: відсутній файл, пошкоджений файл, завелике зображення (через Mock)
- `ImageLoadWorker`: нормальний випадок (помилка не очікується)
- `ImageCanvas.load_image()`: завелике зображення (шлях drag-&-drop)
- `AIWorker`: сигнал помилки при винятку `rembg_remove`, успішний випадок (через Mock)
- Лічильник `_version` canvas: інкрементується при `apply_loaded_image`, незмінний при скасуванні

---

### ✅ 10. Додано підказки типів для значень, що повертаються *(виправлено)*

**Файл**: `BgRemover.py`

77 функцій і методів без анотації значення, що повертається, отримали `-> None` (або конкретний тип). Крім того, до імпорту PyQt6 додано `QFont` (потрібен для `_text_font() -> QFont`).

---

### ✅ 11. Відсутні docstring-и у допоміжних методах *(виправлено)*

**Файл**: `BgRemover.py`

Додано однорядкові docstring-и до `_make_label`, `_make_hdivider`, `_make_panel_btn` і `_make_slider`. Генератори курсорів (`make_wand_cursor`, `make_brush_cursor`, `make_eraser_cursor`) вже мали docstring-и.

---

### ✅ 12. Зробити шлях до файлу журналу платформонезалежним *(виправлено)*

**Файл**: `BgRemover.py`

До імпортів PyQt6 додано `QStandardPaths`. Шлях до журналу змінено з `Path.home() / ".bgremover.log"` на `QStandardPaths.writableLocation(QStandardPaths.StandardLocation.AppDataLocation) / "bgremover.log"` (Linux: `~/.local/share/BgRemover/`, macOS: `~/Library/Application Support/BgRemover/`).

---

### ✅ 13. Усунення дублювання шаблонного коду потоків *(виправлено)*

**Файл**: `BgRemover.py`

Помічник `_launch_worker()` уже було введено в рамках виправлення #3 (стани гонки). Усі три потоки-воркери (Image Load, AI, Warmup) відтоді використовують його.

---

## Раунд 2 – подальший огляд (код, тести, документація, ліцензія)

> Виправлення до раунду 1: пункти **#6 (класи-«боги»)** та **#8 («магічні
> числа»)** були позначені як ✅, але справдилися лише _частково_
> (`MainWindow`/`_build_right_panel` лишався ~300 рядків; чимало
> чисел стилів/компонування лишалися вбудованими). Раунд 2 розглядає
> решту пунктів.

| # | Рекомендація | Пріоритет | Статус |
|---|-----------|-----------|--------|
| R1 | Налаштування журналювання: виклик перед `QApplication`, каталог не створено | 🔴 | ✅ Виправлено |
| R2 | Flood-fill блокує UI; ліміт 100 МП завеликий | 🟠 | ✅ Виправлено |
| R3 | Drag&Drop / «Нещодавно відкриті» обходили асинхронний воркер | 🟠 | ✅ Виправлено |
| R4 | Порушення інкапсуляції (`_pil`/`_version`/`_img_item`/`_cx…`) | 🟡 | ✅ Виправлено |
| R5 | `undo_to` неузгоджений (неможливо відновити) | 🟡 | ✅ Виправлено |
| R6 | `MainWindow` об'єкт-«бог» / `_build_right_panel` | 🟡 | ✅ Виправлено |
| R7 | Немає перевірки типів у CI | 🟡 | ✅ Виправлено |
| R8 | `pyproject` глобально ігнорував `F401` | 🟢 | ✅ Виправлено |
| R9 | `make_tool_icon`: імпорт у циклі, мовчазний `except` | 🟢 | ✅ Виправлено |
| R10 | `_apply_pil` підсумовував стек скасувань O(n) за дію | 🟢 | ✅ Виправлено |
| R11 | Немає захисту від decompression bomb | 🟡 | ✅ Виправлено |
| R12 | Прогалини в тестах (витіснення скасувань, геометрія, ласо, drop) | 🔴/🟠 | ✅ Виправлено |
| R13 | Документація: хибна версія Python, відсутня ліцензія | 🟠 | ✅ Виправлено |

**R1** — Журналювання винесено в `_setup_logging()`; викликається в `__main__`
**після** `QApplication` + `setApplicationName/​setOrganizationName`.
Цільовий каталог створюється через `mkdir(parents=True, exist_ok=True)`
(резервний варіант `~/.bgremover`).

**R2** — `flood_fill` векторизовано (маска схожості у кількох
NumPy-операціях, далі ріст області); `_MAX_MEGAPIXELS` зі 100 → 40.

**R3** — Новий сигнал `ImageCanvas.loadRequested`; `dropEvent` і
`_open_recent` тепер працюють через `_load_image_async` (шлях воркера).
`load_image` лишається синхронним шляхом для тестів/запасного drop.

**R4** — Публічні аксесори: `ImageCanvas.image/has_image/version/
fit_to_view()` і `CropOverlayItem.top_left/size`. `MainWindow` і
`ImageCanvas` більше не звертаються між класами до приватних членів.

**R5** — `undo_to()` поводиться як кількаразове `undo()` (кожен крок
на стек повторення) і тому відновлюється через `redo()`; крім того,
crop-guard як у `undo()`.

**R6** — `_build_right_panel()` є лаконічним диспетчером; чотири
білдери `_build_tab_selection/background/transform/shape` додають по одній
вкладці (індекс вкладки з `addTab()`).

**R7** — `mypy` налаштовано в `pyproject.toml` (прагматично: шум
Qt-override і tuple-lambda приглушено через `disable_error_code`) і
додано як крок CI.

**R8/R9/R10/R11** — Ігнорування `F401` прибрано, два невикористані
імпорти видалено; `make_tool_icon` використовує модульний імпорт `Image`
і логує невдачі через `logger.debug`; поточна сума байтів скасувань
`_undo_bytes` (O(1)); `Image.MAX_IMAGE_PIXELS` прив'язано до `_MAX_MEGAPIXELS`.

**R12** — Нові тести (81 → 108): витіснення за лімітом пам'яті скасувань +
відстеження байтів, `tests/test_geometry.py` (обертання/віддзеркалення/кути/обрізання),
ласо + `_paint_brush` + успішний випадок `apply_remove/replace`,
`tests/test_drop_and_history.py` (асинхронний drop, повторення `undo_to`),
створення каталогу `_setup_logging`.

**R13** — README/INSTALL_MAC: Python **3.10+**; README розширено
архітектурою, відомими обмеженнями, правильним шляхом до журналу та
**розділом ліцензії**; додано `LICENSE` (GPL-3.0); `pyproject.toml` з
`license`/`authors`/`urls`/`classifiers`. Рекомендація щодо ліцензії:
**GPL-3.0-or-later** (відповідає вимозі GPL від PyQt6; дозвільна лише зі
зміною на PySide6).

---

## Раунд 3 – Перед розширенням функціональності

> Два раунди оптимізації завершено; раунд 3 збирає низькоризикові
> чистки, які варто зробити перед запланованим розширенням
> функціональності. Рекомендацію **#1 (моноліт → пакет)** свідомо
> відкладено: високий пріоритет, але також високі зусилля/ризик і
> суперечність із задокументованим рішенням про дизайн «єдиний файл»
> — окреме рішення. Стовпець статусу посилається на PR, що реалізує
> зміну.

| # | Рекомендація | Пріоритет | Зусилля | Статус |
|---|-----------|-----------|---------|--------|
| 1 | Моноліт → пакет (`bgremover/` з модулями) | 🟠 Високий | Високі | Відкрито |
| 2 | ~~`save_image()` без обробки помилок~~ | 🟡 Середній | Низькі | ✅ #48 |
| 3 | ~~Дублювання стану в `undo/redo/undo_to/restore_original/_apply_pil`~~ | 🟡 Середній | Низькі | ✅ #52 |
| 4 | ~~Розкидані вбудовані таблиці стилів, немає модуля теми~~ | 🟡 Середній | Середні | ✅ #53 |
| 5 | ~~Немає хука SessionStart для Claude Code on the web~~ | 🟡 Середній | Низькі | ✅ #51 |
| 6 | Повторювані перевірки «зображення не завантажено» (~8×) | 🟢 Низький | Низькі | Відкрито |
| 7 | Шаблонний код worker-ів (try/except/log/emit) → базовий клас | 🟢 Низький | Низькі | Відкрито |
| 8 | ~~Підтримувати `CHANGELOG [Unreleased]`~~ | 🟢 Низький | Низькі | ✅ постійно |
| 9 | `mypy` дуже дозвільний (7 вимкнених кодів) | 🟢 Низький | Середні | Відкрито |

**#1** — `BgRemover.py` досі є єдиним файлом (~3000 рядків: помічники,
worker, полотно, інтерфейс, діалоги, журналювання, main). Найбільший
важіль для зростання функціональності, але найвищий ризик (ризик:
високий) і суперечність із задокументованим рішенням про єдиний файл.
**Відкрито — свідомо відкладено**, потребує окремого рішення щодо
дизайну.

**#2** — Виправлено в **PR #48**: `save_image()` повертає `bool` і
огортає операції запису в `try/except` (журналювання + повідомлення
стану), узгоджено з `apply_remove/replace`; «Зберегти як…» більше не
запам'ятовує невдалий шлях як ціль швидкого збереження
(`BgRemover.py:1080–1113`).

**#3** — Виправлено в **PR #52** (спочатку #49, чисто відтворено після
конфлікту злиття): ідентичний блок стану зображення об'єднано в
помічники `_set_image_state()` / `_emit_history()`; поведінка незмінна
(`BgRemover.py:877`, `:891`).

**#4** — Виправлено в **PR #53** (спочатку #50): центральна палітра
кольорів `_Theme`, на яку посилаються повторно використовувані шаблони
(перевірено побайтово, 218 таблиць стилів, без візуальної різниці).
Мертві константи `BTN_STYLE`/`GRP_STYLE` видалено (`BgRemover.py:1547`).

**#5** — Виправлено в **PR #51**: синхронний хук `SessionStart`
(`.claude/hooks/session-start.sh`, режим git 100755) встановлює
системні бібліотеки Qt + проєкт і постійно задає
`QT_QPA_PLATFORM=offscreen`; зареєстровано в `.claude/settings.json`.

**#6** — **Відкрито.** Ранній вихід «зображення не завантажено»
повторюється у ~8 методах; невеликий помічник-перевірка згрупував би
це.

**#7** — **Відкрито.** Три потоки worker-ів спільно мають шаблонний
код `try/except/log/emit`; необов'язковий базовий клас зменшив би
повторення.

**#8** — Дотримано: PR раунду 3 #48/#52/#53 кожен підтримує розділ
`CHANGELOG [Unreleased]`; цей запис додатково документує сам раунд 3.
Постійна практика, а не окремий PR.

**#9** — **Відкрито.** `mypy` прагматично послаблено в `pyproject.toml`
(7 `disable_error_code`); поступове посилення покращує безпеку типів
(зусилля/ризик: середні).

---

## Раунд 4 – Оцінка стану та наступний крок

> Стан аналізу: `ruff` чистий, `mypy` чистий, **140 тестів зелені**
> (16 UI-тестів навмисно вилучено). Якість коду висока – тому раунд 4
> пріоритезує **що конкретно робити далі**, а не шукає нові дефекти.

| # | Рекомендація | Пріоритет | Зусилля | Статус |
|---|--------------|-----------|---------|--------|
| 1 | **Зріз версії 2.1.0 + git-тег** | 🟠 Високий | Низькі | **← Наступний крок** |
| 2 | Помічник-перевірка «немає зображення» (раунд 3 #6) | 🟢 Низький | Низькі | Відкрито |
| 3 | Базовий клас worker-а (раунд 3 #7) | 🟢 Низький | Низькі | Відкрито |
| 4 | Поступово посилити `mypy` (раунд 3 #9) | 🟢 Низький | Середні | Відкрито |
| 5 | Моноліт → пакет (раунд 3 #1) | 🟠 Високий | Високі | Відкладено |

### 🟠 1. Зріз версії 2.1.0 + git-тег *(рекомендований наступний крок)*

**Виявлено:** **немає жодного git-тегу** (`git tag -l` порожній), хоча
CHANGELOG стверджує «перший публічно тегований реліз 2.0.0». Від 2.0.0
блок `[Unreleased]` накопичив суттєві зміни (PR #48 обробка помилок
збереження, #52 дедуплікація стану, #53 `_Theme`, документація
INSTALL_LINUX, #55 локальний запускач тестів). `pyproject.toml`
(`version = "2.0.0"`) і резервний `__version__` (`BgRemover.py:51`) досі
показують `2.0.0` – тож поставлений стан не відрізнити від 2.0.0.

**Чому спершу:** найменші зусилля, максимальна ясність. Без зрізу
версії/тегу неможливо відстежити, що було поставлено – це блокує будь-яку
чисту ітерацію функціоналу.

**Кроки:** підняти `version` у `pyproject.toml` і резервний
`__version__` до `2.1.0`; датувати `[Unreleased]` у `CHANGELOG.md`
(+ i18n) як `[2.1.0] – <дата>` і додати новий порожній блок
`[Unreleased]`; поставити та запушити `git tag v2.1.0`.

### 🟢 2. Помічник-перевірка «зображення не завантажено» *(раунд 3 #6, досі відкрито)*

Перевірено: ранній вихід «зображення не завантажено» повторюється у ~10
методах (зокрема `BgRemover.py:1474`, `:1481`, `:2726`, `:2733`).
Невеликий декоратор/помічник-перевірка згруповує це. Малоризиковий
quick win, ідеальний як розминка перед #5.

### 🟢 3. Базовий клас worker-а *(раунд 3 #7, досі відкрито)*

`AIWorker`, `RembgWarmupWorker` та `ImageLoadWorker`
(`BgRemover.py:459–525`) спільно мають патерн `try → logger.exception →
error.emit`. Тонкий базовий клас `QObject` із шаблонним методом
зменшує повторення, не змінюючи модель потоків. Quick win.

### 🟢 4. Поступово посилити `mypy` *(раунд 3 #9, досі відкрито)*

Досі 7 `disable_error_code`. Рекомендація: реактивувати один код на PR
і виправляти тоді видимі влучання, а не все одразу.
Зусилля/ризик: середні.

### 🟠 5. Моноліт → пакет *(раунд 3 #1, навмисно відкладено)*

`BgRemover.py` досі єдиний файл на **3003 рядки**. Найбільший важіль
для зростання функціоналу, але найвищий ризик і суперечить
задокументованому рішенню про дизайн в один файл. Залишається свідомим,
окремим архітектурним рішенням – переглянути щонайпізніше перед
наступним більшим розширенням функціоналу. Quick wins #2/#3 вже трохи
зменшують файл і готують пізніший поділ.
