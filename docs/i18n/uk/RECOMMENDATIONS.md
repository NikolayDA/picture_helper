[Deutsch](../../../RECOMMENDATIONS.md) · [English](../en/RECOMMENDATIONS.md) · [Español](../es/RECOMMENDATIONS.md) · [Français](../fr/RECOMMENDATIONS.md) · **Українська** · [简体中文](../zh/RECOMMENDATIONS.md)

# Аналіз коду та оцінені рекомендації: BgRemover

## Шкала оцінювання

| Символ | Пріоритет | Значення |
|--------|-----------|----------|
| 🔴 | Критичний | Потрібно виправити: призводить до помилок, збоїв або неузгодженості |
| 🟠 | Високий | Варто виправити найближчим часом: суттєво впливає на надійність або підтримуваність |
| 🟡 | Середній | Рекомендовано: покращує якість коду, читабельність або тестованість |
| 🟢 | Низький | Опційно: полірування й додаткові покращення |

---

## Поточний стан (2026-06-02, рев'ю «adoring-johnson»)

Цільове подальше рев'ю після «modest-shannon», з акцентом на шляхи збереження, завантаження та CI. **8 пунктів (N1–N8):** 5 виправлено з регресійними тестами, змерджено через **PR #142** (N1), **#143** (N6, N8) і **#144** (N4, N5); 2 відкриті (N2, N7); 1 уже покрито (N3). База лишається зеленою: ruff/mypy чисті, набір тестів зелений.

### Стан виконання

| Статус | Пункти |
|--------|--------|
| ✅ Виконано | N1, N4, N5, N6, N8 |
| ⏳ Відкрито | N2, N7 |

### Знахідки

- **N1 🟠 — Звільнення блокування чарівної палички на шляху помилки** (PR #142). Продовження «modest-shannon» B: під час зміни зображення `_load_image_async` скасовує flood fill, який тоді не емітує ні `finished`, ні `error`. Скидання блокування відбувалося лише на шляху успіху (`apply_loaded_image`): якщо завантаження зривалося, `_wand_busy` лишався активним, а паличка — заблокованою на старому зображенні. Нова тиха `reset_pending_wand()` просто поруч із `cancel_flood_fill()`.
- **N2 🟡 — Обмеження розміру при повороті** (відкрито). `rotate_image` (`image_ops.py`) повертає з `expand=True`; захист мегапікселів діє лише під час завантаження (`Image.MAX_IMAGE_PIXELS`), а не на результаті. Зображення трохи нижче ліміту може роздутися до ~2× за ~45° — пік пам'яті без запобіжника.
- **N3 ➖ — Бюджет пам'яті історії** (уже покрито). `CanvasHistory` (`canvas_history.py`) давно забезпечує бюджет скасування через `_trim()`/`_UNDO_MEMORY_LIMIT`, а повтор обмежений `_REDO_MAX_ENTRIES`. Дій не потрібно.
- **N4 🟢 — Чесність розширення при збереженні** (PR #144). `save_image_file` мовчки писав байти PNG для невідомих розширень; тепер чітка відмова через `ValueError`, тоді як порожнє розширення лишається PNG за замовчуванням.
- **N5 🟡 — Атомарне збереження** (PR #144). Пряме записування в ціль руйнувало наявний файл у разі переривання. Тепер `mkstemp` → `os.replace` у цільовому каталозі (патерн `qt_plugins._copy_if_needed`), зі збереженням прав і прибиранням тимчасового файлу.
- **N6 🟡 — `libgl1` у повній матриці CI + тест дрейфу** (PR #143). Повна матриця не встановлювала `libgl1` (на відміну від інших джерел Qt-пакетів) → `import PyQt6` ризикував `libGL.so.1`. Додано; новий `test_ci_qt_packages.py` тримає всі чотири списки пакетів узгодженими.
- **N7 🟢 — Раннє імпортування** (відкрито). `workers.py` імпортує `rembg` (тягне onnxruntime) на рівні модуля; оскільки `main_window` завантажує `workers`, вартість імпорту сплачується вже на старті — навіть без використання ШІ. Відкладений імпорт + проба `find_spec` для `REMBG_AVAILABLE`.
- **N8 🟢 — Застарілий docstring `load_image`** (PR #143). Називав шлях drop синхронним викликачем, хоча перетягування давно працює асинхронно. Виправлено.

---

## Відкриті рекомендації

Покращення з другого аналізу, ще не реалізовані (продукт/процес):

- **O1 🟠 — Локалізація застосунку.** UI жорстко німецькою; немає i18n під час виконання (немає `QTranslator`/`tr()`), хоча документація існує п'ятьма мовами. Повідомлення статусу вже централізовані (`status_messages.py`). Поступово через Qt Linguist (`.ts`) або легку таблицю рядків за `QLocale`.
- **O2 🟡 — Linux-застосунок / пакування.** Немає бандла для Linux; запуск лише через `python -m bgremover` із venv. Інсталяційний пакет (AppImage/Flatpak/`.deb`) для **Raspberry Pi OS** і великих дистрибутивів (Debian/Ubuntu/Fedora) знижує поріг входу для не-розробників — аналогічно до macOS-бандла `.app`.
- **O3 🟡 — Повна матриця CI раніше.** Повна матриця (Linux/macOS × 3.10–3.13) виконується лише на тегах/релізах; регресії на macOS чи Python 3.10/3.13 з'являються пізно. Запускати також на push до `main` або щотижневим cron.
- **O5 🟢 — Інтеракційний smoke (qtbot) раніше в CI.** Smoke запуску застосунку (`test_app_smoke.py`) і тести MainWindow/віджетів уже виконуються в PR-gate; лише qtbot-керована інтеракційна сюїта (`test_ui_interactions.py`) обмежена nightly через `-m 'not ui'`. Перенести малий, явно позначений піднабір (напр. маркер `ui_smoke`) до PR/Full CI; повна qtbot-сюїта лишається nightly.

**✅ Виконано з цього раунду (PR #146):** O4 — перемикання інструментів однією клавішею (`W`/`B`/`E`/`L`) із синхронізованим станом toolbar; O6 — tooltips виводять `Cmd`/`Ctrl` залежно від платформи через `_shortcut_label()`. Регресійний тест `test_tool_shortcuts.py`.

## План реалізації пакетами PR (з 2026-06-02)

- **PR 0 — Зміцнення коду (N2 + N7).** Згрупувати два відкриті знахідки з «adoring-johnson»: N2 — застосувати мегапіксельний запобіжник і до результату повороту (обчислити цільовий розмір заздалегідь з кута/діагоналі, зі статусним повідомленням замість необмеженого піку пам'яті); N7 — імпортувати `rembg` відкладено й перевіряти `REMBG_AVAILABLE` через `find_spec`, перенісши gating кнопки ШІ на збій warmup. Мало й малоризиково, без злому UX — перед великими пакетами.
- **PR 1 — Скорочення інструментів і підказки.** ✅ Виконано (PR #146). O4 + O6: перемикання однією клавішею (`W`/`B`/`E`/`L`), синхронізований checked-state toolbar, оновлені tooltips/README/посібник, регресійний тест wiring скорочень.
- **PR 2 — Раніше страхування CI.** O3 + O5: повна матриця також щотижня або на `main`, малий UI-smoke у PR/Full CI, Nightly UI лишається повною сюїтою.
- **PR 3 — Основа i18n.** Підготувати O1: runtime-locale/fallback, поступово централізувати видимі рядки, німецьку лишити стабільним default.
- **PR 4 — Розгортання i18n.** Зробити O1 придатним: щонайменше англійська як runtime-мова, далі інші наявні мови документації, smoke checks для кожної locale.
- **PR 5 — Основа Linux-пакування.** Почати O2: вибрати цільовий артефакт (AppImage/`.deb`/Flatpak), desktop-файл/іконка/AppStream-метадані та Linux build smoke.
- **PR 6 — Розширення Linux-пакування.** Завершити O2: варіант для Raspberry Pi OS, опційний другий формат пакета та release workflow для Linux-артефактів.

---

## Попередні раунди

- **2026-06-01, «modest-shannon» (A–E)** — 5 знахідок, усі виконані (PR #135/#136); серед них обробка decompression-бомб і життєвий цикл палички, який N1 тепер завершує на шляху помилки.
- **v2.2, «admiring-mayer» (#1–#15)** — зовнішній список, #1–#15 виконано, #4 відхилено (хибне спрацювання).

Повні історичні знахідки та робочі журнали (раунди 1–5): [../../history/RECOMMENDATIONS-2026-pre-v2.2.uk.md](../../history/RECOMMENDATIONS-2026-pre-v2.2.uk.md).
